import Products from './Products';

/** Même API `/products` que la page Ventes, libellés et style orientés achats. */
const PurchaseProducts = () => <Products variant="purchase" />;

export default PurchaseProducts;
